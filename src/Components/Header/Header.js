import React from "react";

import "./Header.scss";

const Header = () => {
  return (
    <div>
      <p className="HeaderComponent-uploadTitle">Upload area</p>
      <p className="HeaderComponent-headerSelectTitle">Select a job</p>
    </div>
  );
};

export default Header;
